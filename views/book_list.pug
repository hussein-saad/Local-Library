extends layout

block content
  h1= title

  ul
    each book in book_list
      li
        a(href=book.url) #{book.title}
        if book.author
          |  (#{book.author.name})
        else
          |  (Author not available)

    else
      li There are no books.